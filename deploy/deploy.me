To deploy new release:

1  clone somewhere the heroku repo if not cloned already: https://git.heroku.com/jtools.git
2. build release and copy its files to /app dir
3. git add app/*
4. git commit -m "release x.x.x"
5. git push heroku master